# AURA base runtime dependencies (kept light so the repo clones + imports
# on-device on the VENTUNO Q with only the essentials).
#
# For a full editable install use pyproject.toml instead:
#     pip install -e .            # base only
#     pip install -e ".[vision]"  # + Milestone 1 (Eyes)
#     pip install -e ".[dev]"     # + test/lint tooling
#
# The pins below mirror the sibling VSaaS project's style.

numpy>=1.24
pydantic>=2.7

# ── Per-milestone extras (uncomment as each milestone is implemented) ──
#
# 👁️  Milestone 1 — Eyes (vision): camera capture + YOLO ONNX detection
# opencv-python>=4.9
# onnxruntime>=1.18          # aarch64/Linux CPU EP on VENTUNO Q
# Pillow>=10.0
#
# 👂🗣️  Milestone 2 — Ears/Voice: Whisper STT + MeloTTS
# openai-whisper>=20231117
# sounddevice>=0.4
#
# 📚  Milestone 3 — Memory: vector store (SQLite is stdlib)
# chromadb>=0.5              # or: faiss-cpu>=1.8
#
# 🧠🤖  Milestone 4/5 — Brain/Action: local LLM client + agent tools
# httpx>=0.27               # talks to on-device llama.cpp / Genie server
# langgraph>=0.2
#
# 🖥️  Dashboard: FastAPI + Streamlit UI
# fastapi>=0.115
# uvicorn[standard]>=0.30
# streamlit>=1.36
